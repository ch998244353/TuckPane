Windows 11 x64 离线发行版。

## 2.0.6

- 通用页新增「悬浮展开延时」（100–1500ms）与「离开收缩延时」（200–2000ms）两个滑块：拖动即时生效，重启保留；滑块随对应开关显示/隐藏
- 离开收缩延时同时作用于普通收纳窗与中转站

## 2.0.5

- 修复便签磁贴右键菜单路由：合并去重时误保留了旧版菜单逻辑，导致便签磁贴弹出的是文件菜单（剪切/删除），删除无法作用到便签本体；现恢复上游 2.0 完整路由（便签→重命名/删除专用菜单，含 Station 处理），并保留全部诊断日志
- 随路由恢复，右键菜单的粘贴/新建便签/新建文件夹等入口恢复上游 2.0 完整行为

## 2.0.4

- 收纳盒图标改为**单击打开**（文件/快捷方式/文件夹/便签），拖拽排序不受影响；双击仍可作为兜底
- 便签删除链路增加全程诊断日志，便签不存在时给出明确提示而非静默失败

## 2.0.3

- 修复合并回归：收纳盒内文件/快捷方式双击打开无响应（补回上游双击打开事件）
- 修复拖入语义：从桌面拖入恢复为移动；仅当来源不支持移动（压缩包预览等）时才复制
- 修复拖入文件夹时根目录出现重复文件（拖放事件重复导入）
- 修复粘贴截图生成的文件缺少 .png 后缀
- 便签删除流程加固：窗口关闭失败不再阻断删除，并增加诊断日志
- 图片缩略图仅对真实图片格式尝试，消除 .lnk/.url 的无效报错日志

## 2.0.2

- 合并上游 TuckPane 2.0.2：新增边缘中转站（左/上/右/下贴靠、整边热区呼出、展开置顶不抢焦点）、内置富文本便签（粘贴图片/七种主题/字号/横线/.tucknote 便携文件）、图片文件显示 Windows 原生缩略图（保留宽高比）、悬浮展开/离开收缩/单窗互斥等交互设置、单实例激活重构（支持含空格中文路径）
- 保留 Fork 全部特性：拖入指定文件夹、收纳盒不透明度滑块、锁定位置、收纳窗移屏外防闪、静默启动+启动页、启动计时、DWM 边框防闪烁修复

## 1.0.28

- 移植上游 2.0.0 的文件操作增强：
  - 展开态右键空白处新增「粘贴」与「新建文件夹」：粘贴支持从资源管理器复制的文件（按剪贴板语义复制/移动）、截图位图自动存为 PNG；新建文件夹带名称校验（非法字符/保留名）
  - 磁贴右键新增「剪切」「删除」：剪切写入 Windows 剪贴板语义，可在资源管理器中直接粘贴；删除移入回收站而非直接抹除
  - 拖放导入遵循来源能力：从不支持移动操作的应用（压缩包预览、浏览器等）拖入时自动改为复制，源文件保留
- 修复单实例弹窗标题硬编码版本号

## 1.0.27

- 支持拖放定向导入：展开收纳盒后，把文件直接拖到文件夹磁贴上即可移入该文件夹，悬停时磁贴高亮并在光标旁显示「放入"文件夹名"」提示；拖到空白处或折叠状态仍导入收纳盒根目录。目标文件夹在拖放期间被删除时自动回退为根目录导入。

## 1.0.26

- 新增「收纳盒不透明度」调节：通用页新增滑块（0–100%），调高后毛玻璃收纳盒更实、文字更易读；0% 保持默认效果，设置即时生效并全局应用到所有收纳盒。

## 1.0.25

- 启动改为完全无感：手动启动不再弹出总控台，静默到系统托盘，桌面直接出现收纳盒；托盘图标可随时打开总控台
- 全新安装（还没有收纳窗）首次启动仍会显示总控台，便于新用户上手；创建收纳盒后的启动一律静默
- 通用页新增「启动时显示总控台」开关（默认关），想恢复旧行为的用户可自行打开

## 1.0.24

- 加载页改为静态样式（移除旋转动画）：动画会迫使控制台在收纳窗创建高峰期以每秒 60 帧持续重合成，在混合显卡设备上诱发贯穿窗口的横线伪影；静态加载页将关键期呈现帧数从约 200 帧降至 1 帧。
- 若使用中仍偶见横线（毛玻璃重合成时），属混合显卡驱动层的合成瑕疵，建议更新显卡驱动。

## 1.0.23

- 彻底修复控制台横线闪动：窗口激活状态变化不再重设 DWM 边框属性。此前收纳窗逐个初始化期间，控制台被反复"夺焦又还焦"，每次都触发边框重绘，在混合显卡设备上表现为贯穿窗口的横线伪影；现在边框属性仅在系统主题/设置变化时按需刷新。

## 1.0.22

- 修复控制台加载时的横线闪动：窗口失活时不再重设 DWM 边框（WM_NCACTIVATE），避免收纳窗逐个激活期间顶部边框反复重绘。

## 1.0.21

- 修复启动加载页的横线闪烁：加载期间窗口切换为不透明背景（暂停毛玻璃合成），加载完成后恢复主题背景。

## 1.0.20

- 控制台启动不再白屏：新增「正在加载…」启动页，后台创建收纳窗期间显示加载动画，全部就绪后自动收起。

## 1.0.19

- 彻底消除启动时的窗口闪现：收纳窗在创建后、显示前先移出屏幕外，即使合成器提前绘制首帧也不会被看到，初始化完成后一次出现在最终位置。

## 1.0.18

- 彻底消除启动时的窗口闪现：收纳窗初始化期间桌面层不再提前显示窗口，全部定位完成后一次性出现。

## 1.0.17

- 启动优化：收纳盒在完成桌面层挂接前保持隐藏，不再闪现成普通窗口；开机静默启动时控制台不再弹出。
- 修复：锁定位置的收纳盒恢复时统一使用保存的整体尺寸，不再出现有的大有的小。
- 启动提速：网格读取超时降至 250ms、归一化移到后台线程、图标目录异步刷新，收纳窗先出现、内容渐显；并新增启动各阶段计时日志便于后续调优。

## 1.0.16

- 修复 Steam `.url` 快捷方式显示通用白纸图标的问题，现在读取链接内声明的图标；已有图标缓存会自动升级重取。
- “通用”新增“点击窗口外自动收缩”开关，默认开启；关闭后展开的收纳窗不再因点击别处或失焦而收起。窗外点击不再被拦截，会正常作用于目标窗口，Esc 收缩保持不变。
- 新增保存目录安全检查：拒绝选择磁盘根目录、网络位置、用户主目录、桌面、文档、下载及 TuckPane 数据根目录；新建收纳窗时会检查与其他收纳窗保存目录的重叠。
- 删除使用自定义保存位置的收纳窗前，其保存目录会先整体移动到桌面“（已导出）”文件夹再从原位置删除，防止误删手选目录中的原有文件。

## 1.0.15

- 定位模式收纳盒支持调整整体大小（总控台“入口整体”滑杆，范围 120%–180%）。
- 桌面网格按所选尺寸重新计算占格。

## 1.0.14

- 另存为桌面的新文件图标会自动挪出收纳盒占用的区域，不再被收纳盒压住。

- 安装后自动创建桌面快捷方式。
- portable ZIP 根目录增加 `00-启动 TuckPane.exe`。
- `setup.exe`：当前用户安装，创建开始菜单和桌面快捷方式。
- `portable.zip`：解压后双击 `00-启动 TuckPane.exe`。
- 两者均自带 .NET 与 Windows App SDK。

本发行版尚未进行代码签名，Windows SmartScreen 可能显示“未知发布者”。请使用 `SHA256SUMS.txt` 校验下载文件。
