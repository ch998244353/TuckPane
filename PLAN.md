# TuckPane 4.0.0 更新与发布计划

更新日期：2026-09-12
状态：K0–K3 已完成；用户追加授权直接发布4.0（规范版本4.0.0）；执行K4/K5。此前实际升级手测尚未确认。
本文件是本次执行的唯一计划与进度记录，只覆盖本次对话需求，不继承旧计划任务。

## 1. 已确认的需求与交付边界

1. 设置界面左侧新增“更新”，沿用当前设置样式、语言切换及页面切换时的保存行为。
2. 安装版和便携版都支持应用内更新。自动检查正式 Release；用户点击更新后才下载、保存、退出、安装并重启。
3. 启动后后台检查，每 24 小时至多自动检查一次；发现新版只在侧栏提示，不弹窗打断。更新页保留手动检查入口。
4. 升级原位保留设置、窗口位置、收纳目录关联、真实文件、便签、待办；额外备份配置，不复制全部真实收纳文件。
5. 正式发布包含当前工作树全部已有源码成果和本次新增更新功能，推送源码至现有 GitHub 仓库并发布正式 Release。
6. 原候选版先手测再发布的安排，已由用户最新指令“帮我上传到github,版本是4.0”覆盖：直接发布4.0.0，保留实际升级手测未确认记录，不再等待此项反馈。
7. 禁止电脑、鼠标、键盘模拟以及 GUI 自动测试；实际安装、更新、窗口操作与重启体验由用户验证。
8. 自动测试仅针对本次更新与必要接入边界；不运行旧功能全套测试，不为了覆盖率增加测试，不无依据重复执行。

版本约定：本机已安装3.1.0；2026-09-12远端latest为3.0.2。用户指定4.0，本次统一为4.0.0与v4.0.0。已核对远端v4.0与v4.0.0均未占用，不覆盖既有Release或tag。
发布目标：https://github.com/ch998244353/TuckPane

## 2. 当前架构与修改位置

- 工作容器：`D:\app\功能\TuckPane`；源码及 Git 根：`source`；当前运行版：`app\current`。开发与构建只在源码目录进行。
- `Program.Main / App.OnLaunched` 处理单实例与激活；`AppHost.InitializeAsync` 加载状态，创建隐藏的设置宿主、托盘和各收纳窗口。
- `AppHost` 拥有应用生命周期、状态、窗口集合、文件传输队列；`MainWindow` 承担单个收纳窗、中转站或 Dock 的呈现与交互；便签、待办由独立窗口及文档存储服务处理。
- `ConsoleWindow.xaml/.xaml.cs` 使用左侧 NavigationView。现有系统、显示、右键菜单、交互、主题、收纳窗、中转站、Dock 栏入口；切换页会先保存尚未提交的设置。
- `AppHost.ExitAsync` 负责传输收尾、设置提交、便签与待办保存，再关闭窗口和释放进程。更新必须复用这个退出准备边界，不能直接杀进程或仅调用 Application.Exit。
- `AppPaths` 管理 `%LOCALAPPDATA%\TuckPane` 配置等数据与 `%USERPROFILE%\TuckPane` 收纳数据，也兼容 GlassFolder 旧目录和用户自选收纳目录。便携版目前也使用这些数据根，不改变现有存储语义。
- `StateStore` 已有状态原子保存及滚动 `.bak`；更新前备份需要独立留存，不能只依赖会被后续保存覆盖的 `.bak`。
- `scripts/build-release.ps1` 生成 x64 自包含程序、Inno Setup 安装包、便携 ZIP 与 SHA256SUMS；`.github/workflows/release.yml` 由 v* tag 触发正式发布。
- 版本需同步项目程序集版本、构建脚本默认版本、安装器版本及发布 tag，不能仅改安装包名称。

已完成的只读调研：主 agent 核对架构与设置/退出入口；release_audit 子 agent 核对发布脚本、远端资产、版本及数据路径。
当前会话没有可调用的 CodeGraph 工具，但源码中存在 `.codegraph`；因此调研采用必要源码定点读取，没有初始化或重建索引。后续若工具可用，结构问题优先使用 CodeGraph。

## 3. 执行纪律与子 agent 分工

主 agent 负责需求、架构决策、更新主链路实现、集成以及最终发布协调。子 agent 承担有明确边界的独立工作：

| 时机 | 子 agent 工作 | 交付及限制 |
| --- | --- | --- |
| 调研阶段（已完成） | 发布与数据路径审计 | 只读报告，不读旧 Plan，不运行程序 |
| 主链路接口稳定后 | 审查更新失败/回滚边界，补充或执行少量专项非 GUI 测试 | 只负责约定的更新测试文件，避免与主 agent 同时修改相同文件 |
| 候选版交付前 | 核对 Plan、变更范围、候选包清单及数据保护证据 | 报告具体缺口；复用已有测试输出，不再跑一套相同测试 |

只在主 agent 有独立工作可继续时分配子 agent；不把已完成的探索重新分配，不启动无人使用的研究。

每到下列关键节点，必须重新读取本 Plan 的需求、当前阶段和退出门槛，记录检查结果后才能继续：
- K0：首次修改代码前。
- K1：更新检查、下载与 UI 接通后，进入文件替换实现前。
- K2：保存/退出/备份/安装链路完成后，进入专项验证前。
- K3：候选版打包完成后，交付用户前。
- K4：确认当前有效发布授权后，推送提交及 tag 前。
- K5：远端发布与资产核对完成后，结项前。

发现偏离时暂停对应步骤，修正实现或记录必要的技术调整；涉及用户体验、数据范围或发布授权变化时先向用户澄清。技术细节可在已确认范围内调整，不能静默扩大任务。

## 4. 实施步骤与门槛

### 阶段 A：固定范围与基线（K0）

- 重新读取本计划；记录当前分支、HEAD、dirty 文件清单及版本，不回退、不覆盖已有成果。
- 用户已授权本次合并发布全部已有源码功能；提交前仍逐项分类，排除本机数据、构建输出、备份、缓存、测试产物和 `--clobber/` 旧包。
- 明确安装副本识别、真实数据根和构建依赖；只核对路径和必要元数据，不读取个人便签内容。
- 读取与本次修改相关的项目约束和构建说明；不恢复旧计划，不扩展到旧功能修复。
- 明确更新服务接口及退出成功/取消/失败结果，让专项测试无需启动 WinUI。

门槛：修改范围和数据保护路径已明确；可解释安装版/便携版识别与退出交接方式；不需要旧功能全量基线测试。

### 阶段 B：更新页与版本检查（K1）

- 左侧末尾新增“更新”入口及低打扰的新版提示。
- 页面显示当前版本、可用版本、发布说明、最近检查时间、检查/下载/安装状态及必要错误信息；按钮对应“检查更新”“更新并重启”“重试”等实际状态。
- 沿用现有语言资源及主题机制，不引入新界面风格，不改其他设置页布局。
- 固定查询现有仓库的正式 Release；排除草稿和预发布，采用语义版本比较，版本相同或较旧时不更新。
- 启动就绪后异步检查，不阻塞窗口恢复；持久化自动检查时间，手动检查可绕过 24 小时限制。
- 网络超时、离线、GitHub 限流或资产不完整时保持当前版本可用；自动检查失败不弹窗，详情留在更新页，不无限重试。
- 发布资产须按当前架构和发行形式匹配，不随意选择任意 exe/zip。应用不需要用户 GitHub 登录或内置访问令牌。

门槛：页面接线和保存顺序正确；版本筛选、检查节流、错误恢复有清晰边界；不触发自动下载或自动退出。

### 阶段 C：下载、数据保护与替换（K2）

- 用户点击更新后在独立临时目录下载，显示进度；下载阶段支持取消，防止重复启动多个更新任务。
- 对下载来源、资产类型和 SHA-256 进行检查，核对失败不运行安装器、不替换文件。哈希验证解决下载完整性，不能宣称等同于代码签名验证。
- 完成下载与校验后，通过宿主正常退出准备流程处理传输队列并刷新设置、便签、待办；取消退出、传输未结束或任一保存失败时停止安装，保留运行程序。
- 在保存成功后留存带时间与版本标识的独立配置备份；备份失败则停止更新，明确提示。备份包含实际使用的配置及关联映射，不移动真实文件，也不将便签正文上传 GitHub。
- 外部更新执行器等待当前进程真正退出后才安装/替换；明确准备、确认交接及启动结果，避免主程序退出而更新器未启动的空档。
- 安装版复用现有 Inno Setup，保持 AppId、实际安装目录、用户设置与关联偏好；核对退出码，成功后启动更新后的同一路径。不得由安装器强制终止仍在保存的应用。
- 便携版在程序目录外执行暂存与替换；验证 ZIP 路径，禁止越界写入，使用文件清单区分程序文件与额外用户文件，保留 WebView2 运行数据及未知额外文件；遇到数据目录与目标路径冲突时停止更新。
- 便携版保留本次被替换程序文件的备份；替换失败尝试恢复旧程序并记录结果。安装版依据安装器结果和保留的恢复材料提供恢复路径，不承诺未经验证的事务式自动回滚。
- 磁盘不足、目录不可写、文件被占用、校验失败、安装失败均须给出可恢复状态；不得删除配置或真实收纳目录，不以强杀或递归清空目标目录规避问题。
- 重启成功与否保持可诊断；自动检查不能把重启失败静默标记为更新成功。

门槛：退出确认、备份完成与文件替换的先后顺序可证实；失败停止/恢复行为明确；真实用户数据不作为自动测试输入。

### 阶段 D：精简验证与本地候选版（K3）

只设一个明确的更新专项入口，避免无参数执行 LogicChecks 旧全套。优先少量表驱动用例和真实临时目录，网络使用固定响应替身；不新增沉重测试框架。

必要自动验证分为四组：
1. 版本和资产：新/相同/旧版本、预发布、错误或缺失资产、自动检查 24 小时限制及手动检查。
2. 下载边界：离线/超时、取消、校验不符；不启动安装器，不产生可安装的半成品。
3. 保存与交接：保存失败/退出取消/备份失败时阻止安装，成功时严格按保存→备份→退出→替换交接。
4. 临时文件夹中的便携替换：正常升级、越界 ZIP、替换失败恢复；已有配置及额外文件保持不变。测试运行器不启动 GUI 或真实安装器。

验证规则：
- 实现稳定后执行一次更新专项；只有相关代码变更、测试失败或具体未解决疑点才重跑受影响用例。
- 运行必要的 Release x64 编译和最终打包，复用产物，不对同一份未变代码重复 build/publish。
- 宿主、设置页与安装器连接点做聚焦静态审查；程序启动、实际保存、真实安装与重启仍标记为用户手测。
- 打包检查候选版本、必要运行文件、安装/便携包内容、校验文件及敏感文件排除；不声称这些证明 GUI 工作正常。
- 更新中英文说明与版本发布说明，只描述本次能力及必要使用说明；已有功能只列简明发布摘要，不重新执行旧功能验收。
- 候选产物存入独立目录，提供可点击路径。默认不覆盖当前运行版、不自动关闭或启动用户程序；交付用户安装和测试步骤。

门槛：专项与构建通过，问题报告已收敛，候选包可交付；手测状态明确为“待用户验证”。

### 阶段 E：用户手测与发布授权核对（K4）

向用户提供最短手测清单：
- 设置页左侧“更新”入口、当前版本、手动检查及侧栏新版提示显示正确。
- 安装版点击更新后，保存并正常退出，保留原安装目录，安装完成后重启。
- 便携版更新原解压目录，保留额外文件，完成后重启。
- 更新前后核对窗口布局、设置、目录关联及少量代表性便签/待办，无需重测整个产品。
- 网络失败或取消下载后仍能正常使用；确认配置备份位置及可理解的失败信息。

第一版引入更新器的特殊说明：线上 3.0.2 尚无本次更新入口，候选版 3.1.0 也不能依赖线上一个更高正式版来验证完整升级。交付前必须准备并写清可执行的手测方法，例如在隔离验证目录中准备含同一更新器的较低测试版本及固定候选更新源；测试入口仅限验证构建，不能随正式版开放任意更新地址或误导正式版本比较。用户手测期间不得提前发布正式 Release 来绕过门槛。

用户报告缺陷后，只修复本次更新缺陷，重跑对应专项及必要打包；不重新开展旧功能测试。用户没有回复、仅确认收到或只完成部分检查，都不视为手测通过。最新明确发布授权允许在如实记录未手测的情况下推进发布。

门槛：记录当前有效发布授权及验证状态；本次用户明确授权直接发布4.0.0，不能把该授权或静态审查记为手测通过。

### 阶段 F：推送与正式 Release（K5）

- 再读 Plan，核对用户最新发布授权、候选产物与发布源代码一致性、版本未被占用。
- 核对全部待提交文件；发布当前全部源码成果但排除本地生成包和个人数据，不盲目 git add .。
- 创建清晰发布提交并推送既有目标分支；不强推、不覆盖远端已有版本。
- 推送 v4.0.0 tag，由现有发布工作流完成构建与 Release；先核对 workflow 是否存在旧全量测试或与本次门槛冲突的步骤，必要时只调整本次发布相关部分。
- 发布安装包、便携 ZIP、SHA256SUMS，以及实现需要的程序文件清单等元数据；两种包和版本匹配。
- 验证工作流结果、Release 非草稿/非预发布、latest 指向、资产名称与大小、下载可用性及校验一致性。CI 必要构建属于发布验证，不重复运行旧逻辑测试。
- 如果远端构建的包与已手测候选并非相同字节，记录其共同提交与构建差异；不得宣称远端包已被用户实际手测。
- 将 Release 链接、提交、tag、资产核对结果与剩余限制回填 Plan。

门槛：正式源码与资产可访问，更新接口能识别当前正式版；用户数据未进入仓库；最终报告区分自动验证、用户手测与发布验证。

## 5. 执行记录（持续更新）

| 节点 | 状态 | 证据 / 下一步 |
| --- | --- | --- |
| 需求澄清 | 完成 | 两轮共六项选择已确认，见第 1 节 |
| 架构与发布调研 | 完成 | 当前源码核对；release_audit 只读报告；GitHub latest v3.0.2 |
| 覆盖旧 Plan | 完成 | 本文件，仅本次更新与发布需求 |
| 计划交付核对 | 完成 | 主 agent 核对六项用户回答；子 agent 未发现重大遗漏或冲突；文件编码及冲突标记检查通过 |
| K0 实现前核对 | 完成 | 2026-09-12；main / df994df2a2f9db7111f44f138059a2ea17d01a02；基线清单与 tracked diff 留存 artifacts/update-work；v3.1.0 未被占用；不运行 GUI |
| K1 检查及更新页 | 完成 | 2026-09-12；需求与阶段B复核；24小时检查/手动更新/资源/设置导航编译通过 |
| K2 退出与替换 | 完成 | 2026-09-12；保存/备份/ready/commit/退出/替换边界静态审查完成；补齐排队待办取锁后的守卫；4组更新专项一次通过（2.513秒），不重复旧测试 |
| K3 专项及候选 | 完成 | 2026-09-12；最终生产/验证构建exit0；543个生产程序文件及两种ZIP清单/哈希通过；两种feed与启动脚本核对通过；未运行GUI/安装器 |
| 用户手测 | 待用户反馈 | 已提供独立3.0.9→3.1.0安装/便携手测入口和正式候选检查步骤，见 docs/UPDATE_HANDTEST.zh-CN.md |
| K4 发布前 | 完成 | 2026-09-12；用户直接发布授权已核对；4.0.0构建exit0，543个程序文件和发行包哈希通过；凭据模式扫描无命中；子agent完成文档和文件边界审查 |
| K5 远端发布 | 待执行 | 核对4.0.0源码与产物后推送并发布 |

每次门槛记录至少包含：核对时间、代码/产物版本、检查内容、实际结果、未解决问题及下一步。测试结果不得沿用不同代码版本的成功结论。


## 6. 本次交付与验证证据

- 生产候选：`artifacts/release/candidate-3.1.0/TuckPane-3.1.0-win-x64-setup.exe`、同目录便携 ZIP 和 `SHA256SUMS.txt`；可直接发布的程序目录为 `artifacts/publish/candidate-3.1.0`。
- 最终隔离手测包：`artifacts/update-validation/20260912-011341-8bf2065f`；用户入口为 `start-portable.cmd`、`start-installed.cmd`，不会由 agent 执行。
- `20260912-010734-1fd26379` 是补齐退出守卫前生成的过期验证目录，末尾来源文件校验因生产重建而中断，已写 `SUPERSEDED.txt`；不得用于本次验收。
- 专项唯一执行：`dotnet run --project tests/TuckPane.UpdateChecks/TuckPane.UpdateChecks.csproj -c Release -- --updates`，4/4组通过，exit 0，包含编译耗时2.513秒。记录：`artifacts/update-work/update-checks.txt`。
- 初次锁定还原因新增项目引用失败，已仅更新 `packages.lock.json` 中的共享更新模块项目项，现有包版本保持原样。构建记录保留初次尝试，最终有效日志以以下文件为准。
- 最终生产构建：`artifacts/update-work/candidate-build-final.log`，exit 0。补齐排队待办守卫后重建；没有重复执行已通过的更新核心或旧功能测试。
- 最终验证构建：`artifacts/update-work/validation-build-final.log`，exit 0。两个不同版本仅为首版升级用户手测；验证安装器压缩采用fast，其余生产安装器仍为原压缩设置。
- 产物证据：`candidate-verification-final.json`（生产543个程序文件，清单不一致0，版本3.1.0.0，无验证Handler）、`validation-verification-final.json`（两种ZIP全部文件哈希、四个验证payload清单、feed资产和脚本语法通过），均位于 `artifacts/update-work`。
- 安装器与更新执行器没有被自动运行；没有鼠标/键盘/桌面操作测试；没有覆盖 `app/current` 或读写真实收纳内容。窗口、实际更新、重启与用户数据体验以用户手测为准。
- 后续只能在用户明确手测通过后推进K4/K5；当前没有提交、推送、tag或Release操作。

实现参考（子agent查阅官方资料）：[GitHub latest Release](https://docs.github.com/en/rest/releases/releases#get-the-latest-release)、[资产摘要](https://docs.github.com/en/rest/releases/assets#get-a-release-asset)、[Inno Setup参数](https://jrsoftware.org/ishelp/topic_setupcmdline.htm)、[退出码](https://jrsoftware.org/ishelp/topic_setupexitcodes.htm)、[安装与回滚边界](https://jrsoftware.org/ishelp/topic_installorder.htm)。

## 7. 本机安装（用户追加授权）

2026-09-12 用户要求“帮我安装最新版”，授权在当前安装目录安装正式候选；此要求不等于确认手测通过或授权提前发布GitHub。

- 已确认原安装目录 `D:\app\功能\TuckPane\app\current`、版本3.0.2，安装前无该目录下运行进程。
- 安装包SHA-256通过后，备份将被替换的543个已有程序/安装文件和实际GlassFolder配置。
- 备份目录：`D:\app\功能\TuckPane\app\backups\before-3.1.0-install-20260912-012436`。
- 原位静默安装3.1.0，禁止安装器强制关闭程序或自动重启，退出码0；无需重启Windows。未启动应用。
- 安装后核对543个发布程序文件，哈希不一致0；EXE版本3.1.0.0，安装登记3.1.0；state.json及已有state.json.bak的哈希保持不变。
- 证据：`artifacts/update-work/local-install-context.json`、`local-install-verification.json`及备份目录中的`installer.log`。
- GUI体验、实际应用内升级手测及K4/K5仍待用户反馈；GitHub未发布。

## 8. 4.0 正式发布（最新指令优先）

用户于2026-09-12明确要求“帮我上传到github,版本是4.0”。本节和第1节最新授权覆盖前面历史阶段中“等待手测通过才发布”的限制；该授权不被记为手测通过。

- 对外版本4.0.0、标签v4.0.0，提交包含此前用户已授权纳入的全部本地源码成果。
- 当前main与origin/main同步；标签v4.0.0不存在。保留原本机安装3.1.0，不在本轮自动更换安装版。
- 只修改发布版本、说明及必要发布配置；更新逻辑无变化，复用已有4组专项记录，不重跑旧功能或GUI测试。
- 重新构建4.0.0以核对版本及包内容，再提交源码和tag，使用现有GitHub Actions发布安装包、便携包与校验文件。
- 排除artifacts、bin/obj、本机数据/缓存、旧包--clobber及本地备份。发布结果和下载验证回填本节。


- K4核对结果：4.0.0本机构建exit0，543个程序文件哈希和两发行包校验通过，EXE4.0.0.0，无验证Handler；日志release-4.0.0-build.log、release-4.0.0-verification.json位于artifacts/update-work。准备推送源码和tag，GitHub工作流结果待核对。

